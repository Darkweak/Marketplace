import * as React from 'react';
import { Layout } from "../Common/Layout";
import { LoginForm } from "../_forms/login";

export const Login = () => (
  <Layout container>
    <LoginForm/>
  </Layout>
);
